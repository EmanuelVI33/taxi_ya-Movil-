import 'package:flutter/material.dart';

class SelectCarScreen extends StatelessWidget {
  const SelectCarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Seleccionar Vehículo'));
  }
}
